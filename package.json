{
  "name": "mem-daily-api",
  "version": "1.0.0",
  "description": "Daily memory API for Vercel",
  "main": "api/daily-mem.js",
  "scripts": {
    "dev": "vercel dev",
    "build": "echo 'No build step required'",
    "start": "node api/daily-mem.js"
  },
  "dependencies": {
    "@vercel/node": "^3.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
